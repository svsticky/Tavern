**/.dockerignore
**/.env
**/.git
**/.gitignore
**/.project
**/.settings
**/.toolstarget
**/.vs
**/.vscode
**/.idea
**/*.*proj.user
**/*.dbmdl
**/*.jfm
**/azds.yaml
**/bin
**/charts
**/docker-compose*
**/Dockerfile*
**/node_modules
**/npm-debug.log
**/obj
**/secrets.dev.yaml
**/values.dev.yaml
LICENSE
README.md

# Claude Code session state (worktrees, transcripts) - large and changes constantly,
# irrelevant to any image and would otherwise bust the build cache on every session.
.claude

# Build output/caches - regenerated by each Dockerfile stage, never needed as input.
**/.next
**/.react-router
Frontend/build

# Test project source and its output - not needed to build/run any production image.
Backend.Tests
**/TestResults
**/coverage.cobertura.xml

# Dev-only tooling, not used by any production image build.
.devcontainer
.github